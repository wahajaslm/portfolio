---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Wahaj Aslam | Portfolio Hub" bodyClass="landing-page">
  <style>
    /* Landing Page Specific Styles */
    .landing-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 80vh;
      text-align: center;
      padding: 2rem;
    }

    .hero-wrapper {
      margin-bottom: 3rem;
    }

    .hero-title-large {
      font-size: 4rem;
      line-height: 1;
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
    }

    .hero-subtitle {
      font-family: var(--font-mono);
      color: var(--text-muted);
      font-size: 1.1rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .nav-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      width: 100%;
      max-width: 1000px;
    }

    .nav-card {
      background: var(--bg-card);
      border: 1px solid var(--card-border);
      padding: 2rem;
      border-radius: 12px;
      text-decoration: none;
      transition: all 0.3s ease;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      position: relative;
      overflow: hidden;
    }

    .nav-card:hover {
      border-color: var(--accent);
      transform: translateY(-5px);
      box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.3);
    }

    .nav-card .card-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: var(--accent);
    }

    .nav-card h2 {
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
      color: var(--text-main);
    }

    .nav-card p {
      color: var(--text-muted);
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .nav-card .arrow {
      position: absolute;
      bottom: 2rem;
      right: 2rem;
      opacity: 0;
      transform: translateX(-10px);
      transition: all 0.3s ease;
      color: var(--accent);
    }

    .nav-card:hover .arrow {
      opacity: 1;
      transform: translateX(0);
    }

    @media (max-width: 768px) {
      .hero-title-large { font-size: 2.5rem; }
    }
  </style>

  <div class="landing-container">
    <div class="hero-wrapper">
      <h1 class="hero-title-large mask-reveal active"><span>WAHAJ ASLAM</span></h1>
      <p class="hero-subtitle blur-fade active">Senior Audio Engineer // C/C++ // Python // DSP</p>
    </div>

    <!-- Main Navigation Cards -->
    <div class="nav-grid stagger-parent active" style="margin-bottom: 2rem;">
      <a href={import.meta.env.BASE_URL + '/resume/'} class="nav-card stagger-item">
        <span class="card-icon">📄</span>
        <h2>Resume</h2>
        <p>Detailed professional history, experience, skills, and education.</p>
        <span class="arrow">→</span>
      </a>

      <a href={import.meta.env.BASE_URL + '/projects/'} class="nav-card stagger-item" style="transition-delay: 0.1s;">
        <span class="card-icon">📼</span>
        <h2>Projects</h2>
        <p>Interactive archive of shipped products, research, and personal work.</p>
        <span class="arrow">→</span>
      </a>

      <a href={import.meta.env.BASE_URL + '/mygpt/'} class="nav-card stagger-item" style="transition-delay: 0.2s;">
        <span class="card-icon">🤖</span>
        <h2>MyGPT</h2>
        <p>Ask my AI assistant about my background, coding style, and availability.</p>
        <span class="arrow">→</span>
      </a>
    </div>


    <!-- Info Grid (Teaser + About) -->
    <div class="info-grid blur-fade active" style="transition-delay: 0.25s;">
      
      <!-- FEATURED TEASER -->
      <a href={import.meta.env.BASE_URL + '/projects/my-live-portfolio/'} class="featured-teaser">
        <div>
          <span class="teaser-label">// LATEST_TAPE</span>
          <h3 class="teaser-hook">"My CV had become a stranger."</h3>
          <p class="teaser-sub">The story behind why I built this live system.</p>
        </div>
        <span class="arrow">→</span>
      </a>

      <!-- PORTFOLIO INTRO SECTION -->
      <div class="landing-content">
        <div class="intro-block">
          <h3 class="section-label">// ABOUT_THIS_PORTFOLIO</h3>
          <p class="intro-text">
            Welcome to my digital archive. This site is a <strong>living demonstration</strong> of my engineering philosophy.
          </p>
          <p class="intro-text">
            It contains a detailed catalog of my projects, professional history, and an experimental <strong>AI interface ("MyGPT")</strong> trained on my own knowledge base.
          </p>
        </div>
      </div>

    </div>

    <style>
      /* ... Existing Landing Styles ... */
      .landing-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        /* Center content vertically for the dashboard feel */
        justify-content: center;
        min-height: 100vh;
        text-align: center;
        padding: 4rem 1rem;
        /* ensure sufficient spacing */
      }
      .hero-wrapper { margin-bottom: 1.5rem; margin-top: 0.5rem; }
      .hero-title-large { font-size: 3rem; line-height: 1; margin-bottom: 0.5rem; letter-spacing: -0.02em; }
      .hero-subtitle { font-family: var(--font-mono); color: var(--text-muted); font-size: 0.95rem; letter-spacing: 0.1em; text-transform: uppercase; }
      
      .nav-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; width: 100%; max-width: 1000px; }
      .nav-card { background: var(--bg-card); border: 1px solid var(--card-border); padding: 1.5rem; border-radius: 10px; text-decoration: none; transition: all 0.3s ease; display: flex; flex-direction: column; align-items: flex-start; position: relative; overflow: hidden; }
      .nav-card:hover { border-color: var(--accent); transform: translateY(-3px); box-shadow: 0 10px 20px -10px rgba(0, 0, 0, 0.3); }
      .nav-card .card-icon { font-size: 1.5rem; margin-bottom: 0.75rem; color: var(--accent); }
      .nav-card h2 { font-size: 1.25rem; margin-bottom: 0.3rem; color: var(--text-main); }
      .nav-card p { color: var(--text-muted); font-size: 0.85rem; line-height: 1.4; }
      .nav-card .arrow { position: absolute; bottom: 1.5rem; right: 1.5rem; opacity: 0; transform: translateX(-10px); transition: all 0.3s ease; color: var(--accent); }
      .nav-card:hover .arrow { opacity: 1; transform: translateX(0); }
      /* Info Grid */
      .info-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
        width: 100%;
        max-width: 1000px;
        margin-top: 2rem;
        align-items: stretch;
      }

      /* Featured Teaser */
      .featured-teaser {
        background: rgba(var(--accent-rgb), 0.03);
        border: 1px solid var(--card-border);
        border-left: 3px solid var(--accent);
        padding: 1.5rem;
        border-radius: 8px;
        text-decoration: none;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        position: relative;
        transition: all 0.3s ease;
        height: 100%;
        min-height: 140px;
      }
      .featured-teaser:hover {
        background: rgba(var(--accent-rgb), 0.08);
        transform: translateY(-2px);
      }
      .featured-teaser .teaser-label {
        font-family: var(--font-mono);
        color: var(--accent);
        font-size: 0.75rem;
        letter-spacing: 0.05em;
        margin-bottom: 0.25rem;
        display: block;
      }
      .featured-teaser .teaser-hook {
        font-size: 1.2rem;
        color: var(--text-main);
        margin-bottom: 0.25rem;
        font-weight: 600;
        letter-spacing: -0.01em;
        line-height: 1.2;
      }
      .featured-teaser .teaser-sub {
        color: var(--text-muted);
        font-size: 0.85rem;
        margin: 0;
      }
      .featured-teaser .arrow {
        font-size: 1.2rem;
        color: var(--accent);
        opacity: 0.6;
        transition: all 0.3s ease;
        margin-left: 1rem;
        position: static;
        transform: none;
      }
      .featured-teaser:hover .arrow {
        opacity: 1;
        transform: translateX(3px);
      }

      /* Portfolio Intro (inside grid) */
      .landing-content {
        text-align: left;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
      .section-label { font-family: var(--font-mono); color: var(--accent); font-size: 0.8rem; letter-spacing: 0.05em; margin-bottom: 0.5rem; }
      .intro-text { color: var(--text-muted); line-height: 1.5; margin-bottom: 0.5rem; font-size: 0.9rem; }
      .intro-text:last-child { margin-bottom: 0; }
      .intro-text strong { color: var(--text-main); font-weight: 500; }

      @media (max-width: 768px) {
        .hero-title-large { font-size: 2.2rem; }
        .hero-wrapper { margin-top: 1rem; margin-bottom: 2rem; }
        .info-grid { grid-template-columns: 1fr; gap: 1.5rem; margin-top: 1.5rem; }
        .landing-content { text-align: left; }
        .featured-teaser { min-height: auto; padding: 1.5rem; align-items: flex-start; flex-direction: column; }
        .featured-teaser .arrow { position: absolute; right: 1.5rem; top: 50%; transform: translateY(-50%); margin: 0; }
      }
    </style>
  </div>

  <script>
    // Reveal animations
    document.addEventListener('DOMContentLoaded', () => {
      requestAnimationFrame(() => {
        document.querySelectorAll('.mask-reveal, .blur-fade, .stagger-parent').forEach(el => {
          el.classList.add('active');
        });
      });
    });
  </script>
</BaseLayout>
